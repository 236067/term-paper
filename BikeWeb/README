1.bikeweb: взаимодействует с клиентом, предоставляет серверу информацию о клиенте, получает информацию об обработке с сервера и возвращает ее клиенту.

2.app.config:ip адрес, номер порта, параметры redis, параметры mysql, параметры fastdf соответственно используемые для подключения http//ip:port/, кеш хранения redis, база данных

3.handler:Реализует синтаксический анализ маршрутизации клиента, подключение к серверу, получение данных с сервера и возврат их клиенту.

4.model：Реализация определяет глобальные переменные, необходимые в каждой службе.

5.config:Инициализировать параметры, определенные в модели

6.error：Определение номеров ошибок и сообщений об ошибках

7.misc:

ADddDomain2Url: Соберите URL-адрес изображения аватара через fastdfs.Конкретная роль: после того, как пользователь клиента войдет в систему, он может загрузить свой собственный аватар, и URL-адрес изображения аватара будет объединен с помощью этой функции.

RedisOpen: Операция подключения Redis с параметрами, заданными в файле конфигурации

Getmd5string: Зашифруйте пароль и используйте эту функцию для расшифровки «session» в проекте.

Uploadbybuf: Загружать файлы в соответствии с бинарным потоком, который используется для операции загрузки файлов на сервер (конкретный информационный файл велосипеда может быть загружен продавцом, это может быть видео или другие файлы)

